spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gestion_commandes
    username: root
    password: ""
    url: jdbc:mysql://localhost:3306/gestion_commandes_springboot
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080
    show-sql: true
